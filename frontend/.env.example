# Leave empty in dev — vite.config.js proxies /api to localhost:5000.
VITE_API_URL="/api"

# In production, point at the deployed API:
# VITE_API_URL="https://nudge-api.onrender.com/api"

VITE_ENABLE_CALENDAR="true"
